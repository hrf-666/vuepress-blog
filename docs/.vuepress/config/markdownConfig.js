const markdownConfig = {
    lineNumbers: true, //行号
    // plugins: [
    //     '@org/foo', // 等价于 @org/markdown-it-foo，如果对应的包存在
    //     ['markdown-it-bar', {
    //         // 提供你的选项
    //     }]
    // ]
}

exports.markdownConfig = markdownConfig;
